# Codex Hooks for Alignment Gate

Copy `hooks.json` to your `.codex/hooks.json` to enable the alignment gate system.

These hooks enforce that intent alignment is confirmed before any dispatch command is allowed.
